# Coolify deployment configuration for Playwriter Reflector
# Configure these values when deploying to Coolify

SERVICE_NAME=playwriter-reflector
SERVICE_TYPE=docker
SERVICE_PORT=3000

# REQUIRED: Your public reflector domain (without subdomain prefix)
# Example: reflector.example.com
REFLECTOR_HOST=reflector.example.com

# REQUIRED: Strong random secret for token generation
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=your-strong-random-secret-here-change-in-production

# Node environment (production for Coolify)
NODE_ENV=production

# Optional: Additional configuration
PORT=3000

# Optional: Reverse proxy configuration (automatically handled by Coolify)
# PROXY_ENABLED=true
# PROXY_HOST=reflector.example.com
# PROXY_PORT=443
